{
  "mcpServers": {
    "drama-generation": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/generation/mcp.mjs"],
      "env": {
        "MUSETALK_ROOT": "${MUSETALK_ROOT}",
        "MUSETALK_PYTHON": "${MUSETALK_PYTHON}",
        "MUSETALK_ENTRYPOINT": "${MUSETALK_ENTRYPOINT}"
      }
    }
  }
}
