# Codex 执行合同：character_modify

本文件由所属 Skill 读取，用于约束 Codex 的输入、判断与输出；不是发送给外部文本模型的 system prompt。花括号字段是从当前项目已选资产组装的具名输入槽。

按修改指令更新人物形象，不重设计未被要求改变的身份特征。

【当前形象】
{character_input}

【修改指令】
{user_input}

保留脸型、五官比例、年龄感、体型和永久标志；只修改有明确指令的发型、服装、颜色、材质或配饰。结果仍须是静态全身视觉描述，不写动作、表情、背景、机位、剧情和画风。

只返回严格 JSON：{"prompt":"更新后的完整人物描述"}。使用标准 JSON 转义，不要额外字段。
