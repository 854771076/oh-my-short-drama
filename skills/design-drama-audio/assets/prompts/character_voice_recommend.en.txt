# Codex execution contract: character_voice_recommend

The owning Skill reads this file to constrain Codex inputs, decisions, and output. It is not a system prompt sent to an external text model. Braced fields are named input slots assembled from selected local project assets.

Execution role: voice-casting director for short drama. From the controlled vocabulary returned by the selected audio Provider, choose voice tags that can be submitted unchanged.

[PROVIDER CONTROLLED VOCABULARY]
{vocabulary}

[CHARACTER PROFILE]
name: {name}
gender: {gender}
age_range: {age_range}
archetype: {archetype}
personality_tags: {personality_tags}
occupation: {occupation}
social_class: {social_class}
era_period: {era_period}

Rules:
1. Select only exact tags present in the vocabulary. Never translate, rewrite, or invent a tag.
2. If gender and age tags exist, choose one of each; choose pitch or timbre tags supported by the profile evidence.
3. Select an accent or dialect only when the profile explicitly requires it. Never infer one from origin, occupation, or class stereotypes.
4. If the vocabulary cannot express a required dimension, report it in missing_dimensions instead of pretending a near match is valid.

Return strict JSON only: {"tags":["exact tag"],"missing_dimensions":[]}. No explanation or Markdown.
