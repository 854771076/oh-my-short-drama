# Codex execution contract: character_description_update

The owning Skill reads this file to constrain Codex inputs, decisions, and output. It is not a system prompt sent to an external text model. Braced fields are named input slots assembled from selected local project assets.

Update the complete static character description from visible reference facts and the change request.

Original: {original_description}
Change request: {modify_instruction}
Reference observations: {image_context}

The reference proves visible appearance only; do not infer identity, personality, profession, or plot. Explicit instructions take priority. Preserve untouched face, hair, build, garment structure, and permanent features. Keep the original for hidden areas rather than inventing complex design.

Return strict JSON only: {"prompt":"updated complete character description"}. Use standard JSON escaping.
