# Codex execution contract: location_regenerate

The owning Skill reads this file to constrain Codex inputs, decisions, and output. It is not a system prompt sent to an external text model. Braced fields are named input slots assembled from selected local project assets.

Regenerate three candidates for the same scene.

Scene: {location_name}
Current descriptions: {current_descriptions}

Vary composition and nonessential decoration only. Preserve period, architectural shell, entrances, fixed anchors, material language, and light direction. Default to an empty set and add no plot, people, or unsupported facilities.

Return strict JSON only: {"descriptions":["candidate one","candidate two","candidate three"]}. Use standard JSON escaping.
