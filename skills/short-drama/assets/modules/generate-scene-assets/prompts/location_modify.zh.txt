# Codex 执行合同：location_modify

本文件由所属 Skill 读取，用于约束 Codex 的输入、判断与输出；不是发送给外部文本模型的 system prompt。花括号字段是从当前项目已选资产组装的具名输入槽。

按指令更新同一场景，不重建未要求改变的空间结构。

场景：{location_name}
当前描述：{location_input}
修改指令：{user_input}

保留时代、建筑骨架、出入口、固定锚点和光线连续性；只修改明确指定的材质、陈设、时间状态或公共背景人群。不得新增具名人物、剧情动作或不相关空间。

只返回严格 JSON：{"prompt":"更新后的完整场景描述"}。使用标准 JSON 转义。
