# Codex execution contract: location_description_update

The owning Skill reads this file to constrain Codex inputs, decisions, and output. It is not a system prompt sent to an external text model. Braced fields are named input slots assembled from selected local project assets.

Update the complete scene description from visible reference facts and the change request.

Scene: {location_name}
Original: {original_description}
Change request: {modify_instruction}
Reference observations: {image_context}

The reference proves visible space only; do not infer location history or plot. Preserve untouched structure, entrances, anchors, and light sources. Keep the original for hidden areas. Add no named characters or camera action.

Return strict JSON only: {"prompt":"updated complete scene description"}. Use standard JSON escaping.
