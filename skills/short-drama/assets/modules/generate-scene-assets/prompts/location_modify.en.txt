# Codex execution contract: location_modify

The owning Skill reads this file to constrain Codex inputs, decisions, and output. It is not a system prompt sent to an external text model. Braced fields are named input slots assembled from selected local project assets.

Update the same scene without rebuilding untouched spatial structure.

Scene: {location_name}
Current description: {location_input}
Change request: {user_input}

Preserve period, architectural shell, entrances, fixed anchors, and lighting continuity. Change only explicitly requested materials, furnishings, time state, or public background crowd. Add no named characters, plot action, or unrelated spaces.

Return strict JSON only: {"prompt":"updated complete scene description"}. Use standard JSON escaping.
